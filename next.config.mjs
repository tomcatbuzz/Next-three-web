/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: 'export',
  // module.exports = {
  //   images: {
  //   unoptimized: true
  //   },
  // },

  images: {
    unoptimized: true
  },

  
};

export default nextConfig;
